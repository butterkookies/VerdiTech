# Figma Make — Living Earth UI Redesign

**Date:** June 30, 2026
**Theme:** "Living Earth"
**Aesthetic:** Deep, near-black forest canvas (#0E1512), glassmorphic cards, soft growth-stage gradients, subtle micro-animations.

This file contains the raw Flutter code generated by Figma Make to be integrated into VerdiTech.

## Strategy for Integration
1. Remap `GrowthStage.young` to `GrowthStage.youngPlant` and `GrowthStage.harvest` to `GrowthStage.harvestReady` to align with the real domain enum.
2. Remove `PlantVM` mock classes and use our actual `flutter_riverpod` providers.
3. Migrate `TimelineItem` static entries to use the real `CaEngine` forecasts.
4. Replace `_simulate()` in the CA Grid with the real `_engine.forecast()`.
